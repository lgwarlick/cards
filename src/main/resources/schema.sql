CREATE TABLE CARDRELEASESET(
                               ID LONG NOT NULL AUTO_INCREMENT,
                               NAME VARCHAR(256) NOT NULL,
                               PRIMARY KEY (ID)
);